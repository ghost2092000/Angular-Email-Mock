import { InputtDirective } from './inputt.directive';

describe('InputtDirective', () => {
  it('should create an instance', () => {
    const directive = new InputtDirective();
    expect(directive).toBeTruthy();
  });
});
